# parking-location-intelligence
Make a map that shows how many nearby parking lots, businesses, tourist attractions, restaurants, and venues are around each parking lot.

# running the program
 .\.venv\Scripts\Activate.ps1
 streamlit run app.py
 streamlit run app.py --server.port 8502